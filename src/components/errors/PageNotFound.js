import React from 'react';

import Typography from '@mui/material/Typography';

const PageNotFound = () => {
  return (
    <Typography>Oops! We can't find that page.</Typography>
  )
};

export default PageNotFound;